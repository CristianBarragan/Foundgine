# Foundgine Java samples

The Java sample port starts with the Supply Chain sample. The domain mirrors the canonical sample entities and is intentionally framework-neutral; provider/database wiring is added incrementally as the provider contracts stabilize.
